# vigia — revocations

Rulings that were **withdrawn on purpose**, with who withdrew them and when.

**Why this file exists.** `SPEC.md` holds what is in force and `RULINGS.md` holds the evidence that earned it. Neither can express *this rule is gone*. Without a place to say so, a deletion is indistinguishable from an accident: the acceptance test over the spec reported the first deliberate reversal as a lost ruling, which is a system that can add and cannot subtract — the failure this project spent a week diagnosing, reproduced inside the instrument built to guard against it.

**A revocation is not a correction.** A correction says the rule was always meant to read otherwise. A revocation says the rule was real, was applied, and is now withdrawn — so the entry keeps what it used to say, verbatim, and names who ended it. Unattributed does not bind, and unattributed cannot revoke either.

| Ruling, verbatim | Where it lived | Revoked | By | Why |
|---|---|---|---|---|
| **Branch and commit browsing.** *Same*, where the line above it reads *Reviewer-class, and each would cost an invariant. Use a git client.* Carried in `SPEC.md` §4 as **branch or commit browsing**, among the things that are *Out of v1, deliberately*. | `ROADMAP.md`'s non-goals and `SPEC.md` §4 | 2026-09-10 | reader | Neither line was attributed, which is this project's own test for a session's inference, and [#490](https://github.com/breferrari/vigia/issues/490) is the reader's. An agent that commits its own work empties the pane, which is the complaint `a` answered before [#313](https://github.com/breferrari/vigia/issues/313) and which has been reported twice from use, once by the reader and once by a colleague running the tool beside an agent for days. What is revoked is the browsing half and not the class: nothing here picks a commit to read, and no history is drawn. The pane stands somewhere and shows the working tree from there, which is one comparison rather than a browser, and it costs no invariant: the switch is a first paint against I7 and the steady state is the walk it always was. [#507](https://github.com/breferrari/vigia/issues/507) ships `current` and `since <branch point>`; the list of commits to stand at is [#508](https://github.com/breferrari/vigia/issues/508), and the second reading is [#509](https://github.com/breferrari/vigia/issues/509). |
| **the header's facts are exactly three, the mode set is two** (`watching` and `not watching`), **and there is no changed-line total**. A repository-wide `+`/`-` is a reviewer's summary rather than a monitor's account, it puts first paint back in proportion to the size of the diff against I4, and the only way round that is the wake I1 forbids. | `SPEC.md` §5.1, §10 and §11.1 | 2026-09-09 | reader | Its cost half is measurably false, and `RULINGS.md` had corrected the same reasoning once already: I4 was narrowed on 2026-08-01 because counting is not summing, 8.76ms against 442.71ms. The total is that fold. `hunk::measure` runs for every changed file every tick to give the rows their height and was already computing each group's added lines and discarding them, so §5.2's by-product check, recorded negative when this was ruled, comes back positive. What is left is the taste half, which is the reader's, and he weighed it from use on 0.42.0: [#472](https://github.com/breferrari/vigia/issues/472). |
| **the mode word takes the right alone**. That is a layout ruling rather than a rewording: `watching · 3 files` reads as a verb with an object, naming a curated set that does not exist, and the fix is *adjacency*. | `SPEC.md` §5.1 and §11.1 | 2026-09-09 | reader | Half revoked and half in force, which is why the entry is the clause rather than the row. The adjacency rule stands verbatim in §5.1. What is withdrawn is the word's claim on the side, which the run's line total outranks, and the hazard is removed rather than risked: the word draws only where there is no total, so nothing is ever adjacent to it. [#472](https://github.com/breferrari/vigia/issues/472). |
| **Snap, never ease.** A monitor's premium feel is the state change that lands whole in one frame. Animation needs a clock, a clock needs the timer I1 forbids, and the refusal is not a sacrifice: easing tells a reader the tool is doing something, and this tool's whole claim is that it already did it. | `SPEC.md` §5.3 | 2026-08-27 | reader | Its premise is false by I1's own text. I1 licenses a clock that cannot start on its own, and `RULINGS.md` had **already** corrected exactly this reasoning for the step-button repeat while explicitly declining to carry it across to animation. The surviving half — an *act* answers in one frame — is restated in §5.3; a change arriving may now be drawn arriving. |
| **B8, OSC 8 hyperlinks on drawn paths: no**, and the ground is what it costs the instruments rather than what it costs the reader. Reopen it on `ratatui` carrying a hyperlink as cell metadata rather than as bytes inside the symbol. | `SPEC.md` §11.2 | 2026-08-25 | reader | Overruled inside B18's vocabulary ruling and shipped as [#326](https://github.com/breferrari/vigia/issues/326), on neither of the two conditions it named. The costs it priced were real and are paid rather than avoided, which §11.2 B8 states. Recorded four days late, because the decline sat in force over a feature already shipping and on by default. |
| **B9, a yank key over OSC 52: no**, and the ground is that it would be the first key on this map to destroy something the reader owns. The system clipboard is shared mutable state other programs read. Reopen it on [#72](https://github.com/breferrari/vigia/issues/72) showing that the gap bites. | `SPEC.md` §11.2 | 2026-08-29 | session | On its own condition: [#177](https://github.com/breferrari/vigia/issues/177) reported the gap from the pane. Two of its three reasons had stopped being true and both were mechanism. The dependency reason priced `crossterm`'s `osc52` feature, which nothing has to take. The confirmation reason said a notice *"is cleared by the next `Wake::Tick` and by nothing else"*, which described the loop the day it was written; it folds four deadlines now. The ground that survives is ownership, answered by shape in §11.2 B9. |
| **`Esc` is untouched**, because `Esc` is Quit on every other frame and teaching it to dismiss would put *dismiss this* one keystroke from *end the program*. **`Esc` does not close it, and that is a fact about this keymap rather than about the convention.** `bottom` closes its help with `Esc` and can afford to, because it binds `Esc` to dismissal and never to departure. This pane binds `Esc` to Quit. Adopting the convention would teach a reader that `Esc` means *dismiss the thing in front of me*, one keystroke from the meaning it carries on every other frame. | `SPEC.md` §11.1 and §11.2 | 2026-09-03 | reader | The reader had specified the opposite, and [#341](https://github.com/breferrari/vigia/pull/341) shipped it: `Esc` closes the sheet. What the refusal priced was a key with two meanings, and what shipped is a key with one meaning and a ladder, *leave the frontmost thing*, which the sheet, a drag under way and the program climb in that order. Recorded a fortnight late: the code stopped honouring the ruling and the document went on carrying it, which is the state [#391](https://github.com/breferrari/vigia/issues/391) was filed to end. |
| **B9 — a yank key over OSC 52.** *Ruled 2026-08-15, session: no. Reversed 2026-08-29, session: yes, on the condition the decline set for itself.* ([#177](https://github.com/breferrari/vigia/issues/177), from use; the build is [#372](https://github.com/breferrari/vigia/issues/372), filed apart so the ruling did not wait on it.) See §11.1. | `SPEC.md` §11.2 | 2026-09-03 | reader | The key finished a pointer gesture, and B20's release is that gesture finishing itself: a drag now sends its rows when the button comes up, so `y` was a second thing to reach for. What it uniquely reached, a whole path at the forty columns I6 is named for, a drag over a file heading reaches, gated there by `crates/vigia/tests/select.rs::a_heading_row_sends_the_path_and_not_the_drawn_label`. **The escape is not revoked with the key**: `clipboard.rs` and the ownership shape B9 answered by are what the release inherits, and §11.1 carries them. Removed by [#386](https://github.com/breferrari/vigia/issues/386). |
| **B20 — the shell drawing its own selection over the cells the pointer crosses: no**, and not on cost. A cell holds what was drawn, so a cell selection returns the elided label wherever `render::elide_head` fired, and cannot answer the complaint that produced it. Every terminal this tool targets already performs a better selection behind one modifier. | `SPEC.md` §11.2 | 2026-08-29 | reader | Its ground argues against what shipped and for what it refused. [#177](https://github.com/breferrari/vigia/issues/177) says of the terminal route this recommends that *"it produces exactly the text the terminal has (which is the text on screen, elided marks and all)"*, and two paragraphs earlier that at the widths I6 is named for *"the path is elided, so there is no route to the real text at all"* — the issue raised elision to show the modifier is **not** enough. The ruling used elision to conclude it is. The error underneath is narrower: drawing a selection over painted cells does not oblige the copy to come from those cells, and the program holds the real line text. Withdrawn with the issue reopened; nothing replaces it, because the question is open rather than answered the other way. |
| **`m` shows the masthead and hides it again, and it starts hidden.** Ruled 2026-08-17 from use: *"I see it is not always needed"*, which is the honest read of an element that costs four rows of the thing this tool exists to show. **Off by default since [#204](https://github.com/breferrari/vigia/issues/204)**, by the same reader: the four rows come out of the diff on every pane that can afford the band at all, and an element with that price should be asked for rather than dismissed. An element nobody discovers may as well not exist, which the README's key table pays and the hint bar does not, for the reason at the end of this paragraph. Either way it is a keystroke rather than a setting, which is §11.2 B6 keeping this tool configured by gesture. **Nothing leaves the pane with the band**: the branch is the header's since #158 and is drawn on every frame whatever `m` is doing, which is also what keeps the every-frame `.git/HEAD` read a read of something the frame draws. **A keypress is licensed to move content where a notice is not**, and the rule below already draws that line: the split refuses to move for a *transient* thing so that a flicker cannot jog a reader's diff, and an instruction is not a flicker. Unlike `f` it does not jump, because re-engaging follow is a move (a reader asking to follow is asking to see what changed) and asking for the masthead back is asking for the masthead back. It is deliberately **not** in the hint bar, which #80 cut to three items: `m` is named on the sheet below, where every gesture is written down. | `SPEC.md` §11.1 | 2026-09-08 | reader | The toggle was the compromise that kept the element, and the reader who asked for it has not turned it on since: *"sparkline and heatmap are useful, masthead nah, uses space, I keep it always off."* Discoverability was the ground, and paying it in the README bought nothing an unused element could spend. The masthead is removed by [#457](https://github.com/breferrari/vigia/issues/457) rather than re-defaulted, which takes the band, the `m` key, a layout region and four issues of polish with it. The per-file signals it competed with, the sparkline and the heat strip, ride inside a list row and cost no vertical space; the masthead was a block, and its rows came out of the diff. |
| **beside the stem the answer descends through, which is drawn only where there is an answer to descend through it; and the answer follows one `↳` under that stem, indented.** Ruled 2026-09-07 by the reader from use on 0.38.0, over a two-spine proposal this session made and he refused, and amended 2026-09-08, again by the reader, so the stem waits for an answer rather than promising one. | `SPEC.md` §11.1 | 2026-09-08 | reader | Reported from the pane the same day: *"the arrow doesn't align with the line coming from the input box."* The arrow was not off the stem. At eighty columns the enclosure's corner stood at column 6, the stem's notch at 8 and the arrow at 8, and a gate asserted it. What the eye takes the arrow to hang from is the enclosure's left rule, two rows of it, rather than a one-cell notch at the end of a seventy-column horizontal edge, so the stem bought a third left edge in a stack that reads as one column: the rule, the arrow, and the answer's text two columns right of the words it answers. The replacement is the reader's own and is the whole of it, that the box does not change at all: [#466](https://github.com/breferrari/vigia/issues/466) draws the answer's arrow in the rule itself and its text in the body's column. The two columns the stem reserved go back to the enclosure, which draws at word + 7 where it needed word + 9. |
